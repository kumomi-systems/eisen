[package]
name = "eisen-kernel"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]
test = false
bench = false

[dependencies]

[build-dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"


