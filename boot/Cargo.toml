[package]
name = "eisen-boot"
version = "0.1.0"
edition = "2024"

[dependencies]
uefi = { version = "0.36.1", features = ["alloc", "global_allocator", "panic_handler"] }
eisen-kernel-stub = { path = "../kernel/src/stub" }
